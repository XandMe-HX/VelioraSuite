-- =========================================================
-- VELIORASUITE DATABASE SCHEMA
-- =========================================================
-- Untuk sekarang core clean belum memakai database aktif.
-- Table module akan ditambah satu-satu saat module dibuat.
-- =========================================================

CREATE TABLE IF NOT EXISTS veliorasuite_meta (
    key_name TEXT PRIMARY KEY,
    value_text TEXT
);
